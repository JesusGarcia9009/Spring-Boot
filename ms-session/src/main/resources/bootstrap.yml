spring:
  application:
    name: ms-common-secret, ms-common
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://132.130.152.2:8888}

server:
   port: 9004
   compression:
      enabled: true
   servlet.session:
      # Session timeout after 1 minute
      timeout: 120